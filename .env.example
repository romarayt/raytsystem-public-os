# No API key is required for M0-M5a synthetic qualification.
# Add provider credentials only after an explicit destination-scoped approval.
RAYTSYSTEM_ENV=development
RAYTSYSTEM_CONFIG=config/raytsystem.toml
